server:
  port: 8083
spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true  
management:
  endpoints:
    web:
      exposure:
        include: "*"
info:
  app:
    name: Rating service
    description: Rating Service for microservices
    version: 1.0.0
